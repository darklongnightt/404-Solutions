runtime: php
env: standard
runtime_config:
    document_root: .
env_variables:
    APP_LOG: errorlog
    GOOGLE_STORAGE_BUCKET: "gs://super-data-fyp.appspot.com"
    MYSQL_USER: "root"
    MYSQL_PASSWORD: "superdata"
    MYSQL_DSN: "mysql:dbname=testdb;unix_socket=/cloudsql/try-phpappeng:asia-southeast1:testsql"
beta_settings:
    cloud_sql_instances: "super-data-fyp:asia-southeast1:super-data"